# Simple Interest Calculator

This repository contains a shell script `simple-interest.sh`
that calculates simple interest based on user input.

## Formula
Simple Interest = (Principal * Rate * Time) / 100
